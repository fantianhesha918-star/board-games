# ボードゲームアプリ

## 熟語パズル(docs/jukugo.html)のパズル作成・編集ルール

- 2026-09-23追加。中央が空欄のマスの上下左右に漢字が1字ずつあり、空欄に同じ漢字を1字入れると「上+中央」「中央+下」「左+中央」「中央+右」の4つがすべて実在する熟語(2字の言葉)になる、というパズル。例: 上「社」下「議」左「都」右「見」→中央「会」で「社会」「会議」「都会」「会見」が完成。
- 入力方式は**漢字の候補ボタンをタップして選ぶ多肢選択式**(かな入力や漢字IMEでの直接入力ではない)。理由: 答えは常に漢字1字だが、IME変換に頼ると読み方の統一が必要になり作問の制約が増えるため、候補ボタン方式にして「正しい漢字を見抜く」というパズルの本質だけを残した。候補は正解1字+他の問題の中央漢字からランダムに5字を毎回シャッフルして表示する(`CENTERS`配列から重複なく抽出)。
- パズルデータは`PUZZLES`配列内に直書き(crosswordのような外部単語バンク・自動生成ツールは無し。20問程度の少数構成のため手作業管理)。1件のデータ構造:
  ```js
  {id:1, center:"会", reading:"かい",
    top:{k:"社",w:"社会",r:"しゃかい"}, bottom:{k:"議",w:"会議",r:"かいぎ"},
    left:{k:"都",w:"都会",r:"とかい"}, right:{k:"見",w:"会見",r:"かいけん"}}
  ```
  `w`(熟語)は必ず`top.k+center`(topの場合)のように機械的に連結した文字列と一致させること(逆にすると成立しない)。`reading`は中央の漢字の読み(ヒント表示用)。
- **新しい問題を追加する際の注意**:
  - 4つの熟語すべてが実在する自然な言葉であることを確認する(辞書的に存在しない組み合わせを作らない)。
  - `center`のkanjiは既存の`PUZZLES`内で重複しないようにする(重複すると、その漢字が答えの問題で自分自身が紛れ込む形にはならないが、候補プールの多様性が減るため)。
  - 4方向の熟語の読みが完全に一致している必要はない(音読み/訓読みが混在してもよい。実際の日本語がそうであるため)。ただし`reading`フィールドには中央漢字の最も代表的な読みを1つ選んで入れる(ヒントの「最初の1文字」に使われる)。
  - 追加後は`w`が`k`+`center`(またはその逆)の機械的連結と一致しているか、簡単なnode -eスクリプトで検算してから確定すること(2026-09-23の実装時に使った検証パターン: `PUZZLES`をevalで取り出し、各方向で`combine!==w`ならエラー表示)。
- 難易度分けは無し(crosswordと同様、全問1リストで表示する方針)。
- ヒントは2段階: 1回目で読みの最初の1文字を表示、2回目で答えそのものを表示(hintUsed扱いとなり、クリア記録に残る)。
- 統計は`BGStats.record("jukugo", ...)`で記録。`cur.plays`/`cur.clears`/`cur.perfect`(ノーミス・ヒント無しクリア回数)を使用。クリア済み問題一覧は`localStorage`の`jukugo-solved`(配列)、総問題数は`jukugo-meta`に保存(crosswordの`crossword-solved`/`crossword-meta`と同じ命名パターン)。
- 新しいゲームファイルを追加した際は、`docs/index.html`(ゲームカード+ルーティング)、`docs/stats.html`(アイコン+カード関数+render()への追加)、`docs/rules.html`(nav.tocリンク+ルールセクション)、`docs/sw.js`(CORE配列への追加+VERSION更新)の4箇所すべてに反映すること(2026-09-23、jukugo追加時に確立した手順)。README.mdのページ構成・収録状況も更新する。

## クロスワード(docs/crossword.html)のパズル作成・編集ルール

- `PUZZLES`配列に問題を追加・編集したら、必ず`node tools/check_crossword.js`を実行してから完了とすること。
- このスクリプトは以下を検証する:
  - 他のどの単語(across/down)とも交差しない「孤立ワード」がないか(2026-09-21、一部の問題で黒マスを減らした際に他の文字と一切接しないマスが発生した不具合が見つかったため追加)
  - グリッド文字列と`answer`が一致しているか
  - 採番(`num`)が同一方向(across/down)内で重複していないか
  - どの単語にも属さないマス、2マス以上連続しているのに対応するクルー定義がないマス列がないか
  - `pickup`(ボーナス文字)の位置が`pickupAnswer`と一致しているか
- 「問題なし」と表示されるまで、孤立ワードが見つかった問題は交差する単語を追加するか、別の単語に差し替えて修正すること。
- 選択画面(`puzzleList`)には`theme`を表示しない(2026-09-21、答えが書かれてしまうため削除済み)。`theme`欄自体は内部メタデータとして残しているが、UI上は使われていない。
- **難易度フィルターは廃止済み**(2026-09-21)。以前あった「やさしい/ふつう/むずかしい/鬼」の絞り込みUI・`level`フィールド・レベル別統計は全て削除し、全問題を1つのリストで表示する方式にした。今後新しく問題を追加する際も`level`フィールドは付けないこと。
- **新しい問題は`node tools/generate_crossword.js [問題数] [列サイズ...]`で自動生成できる**(2026-09-21追加)。`tools/wordbank_adult.json`(大人向け単語バンク)を単語プールとして使い、標準的なクロスワード作成アルゴリズム(種語を配置→他の単語を既存マスに交差させながら追加)でグリッドを組み立てるため、孤立ワードは構造上発生しない。例: `node tools/generate_crossword.js 4 8x8 9x9 7x9 9x7` で4問生成しPUZZLES配列末尾に自動追記される。生成後は必ず`node tools/check_crossword.js`で検証すること。手作業で問題を作る場合よりもこちらを優先して使う。
  - 語彙を増やしたいときは`tools/wordbank_adult.json`に`{"answer":"...","clue":"..."}`を追記していく(ひらがな表記、小さい「ゃゅょっん」も1マス1文字としてカウント。カタカナ・固有名詞・助詞を含むフレーズは避け、実在する単語・熟語かを必ず確認してから追加する)。
  - **クロスワードの対象は大人**(2026-09-21、ユーザーが明言。用途は「暇つぶしと脳トレ」)。既存のid1〜52は子ども向けひらがな問題のままでよい(削除しない)が、今後`generate_crossword.js`で新規生成する問題はすべて大人向け語彙(`tools/wordbank_adult.json`)のみを使う。`tools/wordbank_extra.json`(子ども向け+一部応用語彙)は過去の名残として残しているが、`generate_crossword.js`は参照していない。
  - 難易度の変遷: 旧鬼レベル(30マス・9〜14語・最長4〜5文字)→8x8〜9x9(id53〜70)→11x11〜12x12(id71〜80、四字熟語・理科社会用語を追加)→大人向けバンクへ全面切替(id81〜、8x8〜12x12混在、慣用句・仕事/経済・文化・健康などの語彙)。
  - さらに難化・拡充させたい場合は列サイズを`13x13`などさらに大きくするか、`tools/wordbank_adult.json`に大人向けの長い熟語・慣用句を追記する。既存の長い単語(6文字以上)を種語として使い切ると生成失敗率が上がるので、長め単語(6文字以上)を優先して増やすとよい。
  - **テーマ別生成もできる**(2026-09-21追加、「違うパターンで作って」というユーザー要望に対応)。`--theme=カテゴリ名`を付けると、そのカテゴリの単語+短い汎用語(2〜3文字の接続用の言葉)だけで1つの話題にまとまったパズルを作る。例: `node tools/generate_crossword.js 2 9x9 --theme=kanyouku`。カテゴリは`tools/wordbank_adult.json`の各語の`cat`フィールドで管理しており、現在は`shizen`(自然)・`gourmet`(食・酒)・`shigoto`(仕事・経済)・`kokoro`(こころ)・`shikou`(思考)・`bunka`(文化・趣味)・`kenkou`(健康)・`kanyouku`(慣用句・四字熟語)・`seikatsu`(生活、カテゴリ未指定語のデフォルト)がある。テーマ付きで生成した問題はタイトルに`クロス93(慣用句)`のようにテーマ名が自動で入る。
    - カテゴリごとに長い単語(6文字以上)の在庫に差があり(慣用句は34語と豊富、思考・健康・生活は0語)、少ないカテゴリでは9x9のような大きい盤面は失敗しやすい。`gourmet`(食・酒)は7x7〜7x8程度が安定する。新しいカテゴリの語を追加するときは、できれば6文字以上の単語も何語か含めるとより大きい盤面が作れる。
    - 新しいカテゴリを増やしたいときは、`tools/wordbank_adult.json`の該当する語に`"cat":"新カテゴリ名"`を追記していけばよい(専用の登録スクリプトはなく、直接JSONを編集する)。
- **ボーナス文字(pickup)は5〜10文字の単語から作る**(2026-09-22変更)。以前は2文字固定だったが、`addPickup()`を「グリッド上の文字を寄せ集めて5〜10文字の単語にする」方式に変更(見つからない場合は4文字→2〜3文字の順に条件を緩めるフォールバックあり)。
- **黒マスの多さは現在のアルゴリズムの構造的な限界**(2026-09-22判明)。「交差点以外はマス同士を絶対に接触させない」というルールのため、密に単語を詰め込んでも黒マス率は45〜50%程度で頭打ちになる(実験: 偶然できた並びも辞書に実在すれば単語として登録する改良を試したが、黒マス率はほぼ変わらなかった)。これ以上減らすには単語を1つずつ交差させていく現方式ではなく、全マスを同時に解く本格的なクロスワード自動生成(パターン先決め+充填ソルバー)への作り直しが必要で、大掛かりな変更になる。今のところは「詰め込めるだけ詰め込む」(`stalledRounds`が続く限り単語を置き続ける)方式で妥協している。
- **既存の自動生成問題を作り直す運用**(2026-09-22)。ユーザーから生成アルゴリズムの改良依頼があった際、「今すでにある自動生成問題も作り直すか、今後の新規生成だけに適用するか」を確認したところ「既存も作り直す」を選んだ実績あり。該当id範囲のパズルブロックを`docs/crossword.html`から削除してから`generate_crossword.js`を再実行すると、削除後の最大id+1から自動的に採番される。
- **問題選択画面は20問区切りのタブ(アコーディオン)表示**(2026-09-22追加、「縦にずらっと並んで選びづらい」との指摘で変更)。`renderPuzzleList()`は`PLIST_GROUP_SIZE=20`ごとに`.pgroup`(見出し`.pgroup-head`+本体`.pgroup-body`)を生成し、見出しタップで開閉する。開閉状態はモジュールスコープの`openPuzzleGroups`(Setで管理、初期値は先頭グループのみ開く)に保持し、再描画をまたいで維持される。
- **カテゴリはkanyouku(慣用句)・shizen(自然)・gourmet(食・酒)・seikatsu(生活)・kokoro(こころ)・bunka(文化・趣味)・shigoto(仕事・経済)・shikou(思考)・kenkou(健康)の9種類全てにテーマ別問題あり**(2026-09-22時点、各カテゴリ2〜4問)。「色んなジャンルの問題を追加して」と言われたら、まだ手薄なカテゴリ(語彙が少なめのshikou・kenkou・seikatsuなど)を優先しつつ、`--theme=`で各カテゴリに追加していくとよい。
- **黒マス20〜30%台の本格ソルバー方式を実装済み**(2026-09-23〜、[[crossword-black-square-limit]]参照)。語彙拡充だけでは黒マス削減が進まないと判明した後(261語→3000語まで増やしても成功率が横ばいだった経緯は下記参照)、ユーザー提案で**アルゴリズム自体を作り直した**結果、実用的な黒マス20〜30%台の生成に成功するようになった。`node tools/generate_crossword.js [問題数] [サイズ...] --solver`で有効化する新モード。
  - 仕組み: (1)黒マスパターンを「逐次構築方式」で作る — 1マス(180度対称の1ペア)ずつランダム順で黒マス化を試し、その場で`validPattern`(孤立白マス禁止)を満たすか即座にチェックする。旧来の「完全ランダムに配置してから検証、ダメなら最初からやり直し」方式は9x9以上でパターン生成自体がほぼ失敗していたが、この方式だと全サイズでほぼ100%パターンが作れる。(2)充填(単語当てはめ)は「1つのパターンに長時間かけて粘る」のではなく、**複数の異なるパターンを短時間(`--attempt`ミリ秒、既定500ms)ずつ次々試し、ダメならすぐ次のパターンへ切り替える**方式にする(`--budget`ミリ秒、既定20000ms=1問あたり20秒の合計予算)。この2点の組み合わせが最も効いた実験結果(例: 9x9・黒マス30%で成功率0%→100%、10x10で0%→90%)。
  - 黒マス密度は`--black=0.25`などで指定でき、省略時は盤面サイズに応じて自動選択(7x7以下は25%、それ以上は30%)。**黒マス20%は現状の語彙(5000語)でも成功率0%のまま**なので、20%を狙わせないこと。黒マス22〜24%も8x8で稀に(30秒予算で1/4程度)成功する程度で、実用的な成功率には届かない。
  - **11x11以上への対応(2026-09-23追記)**: forward-checking(交差スロットの候補が0にならないか仮置き直後にチェックする制約伝播)を試したが、同一条件(黒マス30%・15秒予算・8試行)でbaseline/FCとも2/8成功と完全に同率で、有意な効果はなかった(不採用)。一方**計算予算を伸ばすと素直に成功率が上がる**ことが判明(15秒予算で25%→60秒予算で50%)。11x11は「解なし」ではなく単に探索量不足だっただけ。これを受け、`--budget`未指定時は101マス超(11x11以上)で自動的に60000ms(それ以外は20000ms)を使うようデフォルトを変更し、さらに1回の予算で失敗したらseedBaseを変えて最大3回まで自動再挑戦する仕組みを追加した。id137・138がこの改修後に初めて成功した11x11の実例(いずれも黒マス30%、語数60)。
  - **既存id53〜132(大人向け旧方式80問)は2026-09-23に`--solver`方式で全問作り直し済み**。id1〜52(子ども向け)は対象外でそのまま。元のサイズ構成(8x8×15、9x9×6等)を保ったまま削除→再生成し、id53〜132として振り直された(結果的に元と同じid範囲になった)。作り直し後の平均黒マス率は約29.9%(旧方式の45〜52%から大幅改善)。今後の新規生成でも`--solver`を使うことが推奨。11x11以上も`--solver`で生成可能(時間はかかる、1問あたり最大3分程度)。
  - **大サイズ(12x12など)は成功率が低いことに注意**: 12x12(144マス)は予算90秒×3回再挑戦でも成功率2〜3割程度と低く、作り直し時は目標数に届かず、確実なサイズ(11x10等)で穴埋めして帳尻を合わせた実績がある。12x12を大量に作りたい場合は、生成に失敗する前提で余分に試行回数を確保すること。
    - **原因を診断済み(2026-09-23)**: 「長い単語の語彙不足が原因」「1回の試行時間(perAttempt)配分が悪い」の両仮説を検証したが、どちらもハズレだった(手詰まりの大半は短い長さ2〜4のスロットで発生、perAttemptを500ms→10000msに伸ばしても同一予算で成功率は変化なし)。**スロット数が多い(76語前後)ことによる純粋な組み合わせ爆発が原因**で、語彙追加やパラメータ調整では改善しない。本格的に解決するには制約伝播(AC-3等)やSATソルバー的アプローチへの作り直しが必要だが、費用対効果が低いため保留中。
  - **重要な注意**: `tools/generate_crossword.js`は`docs/crossword.html`を丸ごと読み込んで追記し書き戻す方式のため、**2つのgenerateプロセスを同時に並行実行すると書き込みが競合し、後勝ちで片方の追加分が消える**(2026-09-23に実際に9x9・8x8の2問が一時的に消失する事故が発生、再生成で復旧)。複数サイズ・複数問を生成したいときは1回のコマンド呼び出しに全部まとめて渡すか、必ず前のコマンドの完了を待ってから次を実行すること。
  - **`PUZZLES`配列を手動でスクリプト等から書き換えるときの注意**: `generate_crossword.js`は常に「最後の要素にも末尾カンマを付ける」形式(`{...},\n  ];`)でファイルを保持している。この前提が崩れる(末尾要素にカンマがない状態で保存する)と、次に`generate_crossword.js`が新しいブロックを直前に挿入した際、直前の要素とのあいだにカンマが無く`Unexpected token '{'`の構文エラーになる(2026-09-23、id53〜140を一括削除する自作スクリプトでこの事故が発生、手動でカンマを1つ追加して復旧)。パズルを手動削除・並べ替えする際は、必ず配列の最後の要素にも末尾カンマを残すこと。
- **黒マス本格削減に向けた語彙拡充だけでは限界という結論**(2026-09-22〜23の経緯)。現行の「1語ずつ交差させる」旧方式は構造上、黒マス率45〜50%が限界。ソルバー方式なら黒マスを減らせると考え、`tools/wordbank_adult.json`を261語から**5000語**(2026-09-23時点、+4739語・約19倍)まで拡充したが、旧来の単純な試作ソルバー(ランダムパターン生成+単一パターンに時間をかける方式)では成功率は7x7が2/30・8x8が1/20程度のまま、1763語版から5000語版までほぼ横ばいだった。9x9・10x10は充填失敗以前に「黒マスパターン自体の生成」がランダム探索で毎回失敗しており、これは語彙不足ではなく試作ソルバーのアルゴリズム自体の限界だった。**この規模まで語彙を増やしても改善しなかったため、語彙拡充だけによる黒マス削減の実用化は見込みなしという結論に達し、上記のアルゴリズム作り直しに舵を切った**(結果は上記の通り成功)。「大人向け語彙は今後クロスワード以外のゲームにも使う」方針にユーザーが変更したため(2026-09-22〜)、語彙増量自体は他ゲーム用の汎用資産として引き続き価値があるが、黒マス削減の観点では今はアルゴリズム側の改善(11x11対応、黒マス20%台前半への対応)の方が優先度が高い。追加時は1バッチずつ小刻みに区切らず、まとめて大きめのバッチ(100〜2000語程度)で追加してよい(2026-09-23、ユーザー了承・実績あり)。**大規模一括追加をする際の注意**: 既存語彙が2000〜5000語規模になると、身近な基礎名詞(動植物・食器・行事名など)はすでにかなり網羅されており重複率が非常に高い(下書き355語中68%が既存と重複した実績あり)。新規率を上げるには、四字熟語・ことわざ/慣用句表現・専門用語(理科/医療/業界用語・職業名)、さらには「あさの〜」「いちにちの〜」等の複合フレーズ的な語など、より対象を絞った具体的な語彙を狙うと新規率90%超も達成できる(実績: 2026語下書き→2000語が新規=約99%)。追加時は必ず既存語彙全件と自動突合してから候補を確定すること。また「ー」(長音記号)は`ぁ-ゟ`のひらがな範囲外なので、カタカナ語(スープ・テープなど)をひらがな化する際は長音を含まない別の言い回しに置き換えるか、その語自体を避けること(2026-09-23、複数回この理由で差し戻しが発生した)。辞書データ(UniDic/JMdict等)のダウンロードは、出典表示義務(CC BY-NC-SA/GPL系ライセンス)がありコストに見合わないため使わない方針(2026-09-22、ユーザーと合意済み)。

補足: 「交差は1箇所だけの制約があるのか」という疑問に対しては、そのような制約はなく実際に1単語が複数箇所で交差することもある(2026-09-22実データで確認)。黒マスが多い根本原因は、白マスのうち「ヨコ・タテ両方の単語に属する真の交差点」が約40%しかないこと(残り60%は片方の単語にしか属さない)。旧方式でこの交差点率を上げるには大量の語彙が必要だったが、`--solver`方式はパターンを先に決めてから充填するため、この制約から根本的に解放されている。
